---
argument_specs:
  template-ansible-role:
    author: "xenion1987"
    description: "A template to create Ansible roles"
    # Variable documentation is maintained in defaults/main.yml
    # via @var annotations and rendered by ansible-doctor.
    #
    # Populate options{} only when runtime argument validation
    # (type checks, required fields, choices) is needed.
    options: {}
